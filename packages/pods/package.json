{
	"name": "@casemark/linc-pods",
	"version": "0.1.0",
	"description": "CLI tool for managing vLLM deployments on GPU pods",
	"type": "module",
	"bin": {
		"linc-pods": "dist/cli.js"
	},
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js && shx cp src/models.json dist/ && shx cp -r scripts dist/",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"files": [
		"dist",
		"scripts"
	],
	"keywords": [
		"llm",
		"vllm",
		"gpu",
		"ai",
		"cli"
	],
	"author": "CaseMark",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/CaseMark/linc.git",
		"directory": "packages/pods"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"dependencies": {
		"@casemark/linc-agent-core": "^0.1.0",
		"chalk": "^5.5.0"
	},
	"devDependencies": {}
}
